# --- Add these to your Laravel project's .env ---

# Where the React app is served from (used for CORS)
FRONTEND_URL=http://localhost:5173

# First admin login, used by DatabaseSeeder — change the password before your first seed run
ADMIN_NAME="Marc"
ADMIN_EMAIL=admin@mecnetworks.com
ADMIN_PASSWORD=change-me-before-first-run

# Broadcasting (real-time updates)
BROADCAST_CONNECTION=reverb

# These 5 are generated automatically the first time you run:
#   php artisan install:broadcasting
# (it edits .env for you) — shown here so you know what to expect.
REVERB_APP_ID=
REVERB_APP_KEY=
REVERB_APP_SECRET=
REVERB_HOST="localhost"
REVERB_PORT=8080
REVERB_SCHEME=http

# Same values, prefixed for the React (Vite) frontend to read
VITE_REVERB_APP_KEY="${REVERB_APP_KEY}"
VITE_REVERB_HOST="${REVERB_HOST}"
VITE_REVERB_PORT="${REVERB_PORT}"
VITE_REVERB_SCHEME="${REVERB_SCHEME}"
